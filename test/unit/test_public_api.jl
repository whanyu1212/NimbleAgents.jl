###############################################################################
# test_public_api.jl — exported API contract
###############################################################################

@testset "Public API export contract" begin
    expected = Set(
        Symbol[
            Symbol("@tool"),
            :AbstractMemoryService,
            :AbstractSessionStore,
            :AbstractTool,
            :Agent,
            :AgentHooks,
            :ApprovalTimeout,
            :Artifact,
            :Block,
            :CLIArg,
            :CLITool,
            :ContextConfig,
            :EvalCase,
            :EvalReport,
            :EvalResult,
            :ExternalAgentTool,
            :GeminiOpenAISchema,
            :Guardrail,
            :Handoff,
            :HandoffFilter,
            :HumanInterrupt,
            :InMemoryMemoryService,
            :InMemorySessionStore,
            :JSONSessionStore,
            :MCPClient,
            :MCPHTTPClient,
            :MCPServer,
            :MemoryEntry,
            :Modify,
            :NimbleAgents,
            :NimbleTool,
            :Pass,
            :RateLimiter,
            :RetryConfig,
            :SQLiteMemoryService,
            :SQLiteSessionStore,
            :Session,
            :Skill,
            :Tool,
            :ToolEvent,
            :ToolMessage,
            :Trace,
            :TurnEvent,
            :add_memory!,
            :agent_as_tool,
            :bash_tool,
            :build_tool_map,
            :chat!,
            :claude_code_tool,
            :cleanup!,
            :close!,
            :codex_tool,
            :compact!,
            :connect!,
            :cost_budget,
            :delete_file_tool,
            :delete_memory!,
            :discover_skills,
            :dispatch_tool,
            :edit_file_tool,
            :eval_julia_tool,
            :exact_match,
            :fan_out,
            :fetch_webpage_tool,
            :find_files_tool,
            :fuzzy_match,
            :get_model_pricing,
            :github_trending_tool,
            :glob_tool,
            :grep_tool,
            :handoff_tool,
            :http_get_tool,
            :http_post_tool,
            :latency_budget,
            :list,
            :list_dir_tool,
            :list_memories,
            :list_tools,
            :load,
            :load_eval,
            :load_trace,
            :loop_pipeline!,
            :print_eval,
            :print_trace,
            :read_file_tool,
            :recall_memory_tool,
            :register_artifact!,
            :remove_model_pricing!,
            :remove_rate_limit!,
            :reset!,
            :resume!,
            :run!,
            :run_eval,
            :run_pipeline!,
            :save!,
            :save_artifact_tool,
            :save_eval,
            :save_memory_tool,
            :save_trace,
            :search_memory,
            :serve,
            :set_model_pricing!,
            :set_rate_limit!,
            :spawn_subagents,
            :store_artifacts_dir,
            :tool_coverage,
            :tool_trajectory,
            :tools_schema,
            :write_file_tool,
        ],
    )

    actual = Set(Base.names(NimbleAgents; all=false, imported=false))
    @test actual == expected
end

@testset "Removed compatibility surface stays removed" begin
    @test !isdefined(NimbleAgents, :PT)
    @test !isdefined(NimbleAgents, :DataMessage)
end
